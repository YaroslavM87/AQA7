# Use postgres/example user/password credentials
    version: '3.1'

    services:

      db:
        image: postgres
        restart: always
        ports:
          - 8080:8080
        environment:
          - POSTGRES_USER: student
          - POSTGRES_PASSWORD: aqahw7        
