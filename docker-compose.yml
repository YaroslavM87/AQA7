version: '3.0'

services:

  db:
    image: postgres
    restart: always
    environment:
      - POSTGRES_DB: db
      - POSTGRES_USER: student
      - POSTGRES_PASSWORD: aqahw7      

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
